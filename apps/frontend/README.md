# Frontend

Vite + React 前端，第一版只有一个股票代码输入页。

## Local

```bash
npm install
npm run dev
```

本地默认调用 `http://localhost:8000`。线上部署到 Vercel 后，在项目环境变量中设置：

```text
VITE_API_BASE_URL=https://your-backend.onrender.com
```

